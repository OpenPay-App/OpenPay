# Promtail config — scrapes Docker container logs and ships them to Loki.
# Mounted into the promtail container at /etc/promtail/config.yml.
# Requires the Docker socket mount (already configured in docker-compose.yml).
# See docs: /docs/self-hosting/monitoring

server:
  http_listen_port: 9080
  grpc_listen_port: 9095

positions:
  filename: /tmp/promtail/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
    relabel_configs:
      - source_labels: ["__meta_docker_container_name"]
        regex: "/(.*)"
        target_label: "container"
      - source_labels: ["__meta_docker_container_label_com_docker_compose_service"]
        target_label: "service"
      - source_labels: ["__meta_docker_container_log_stream"]
        target_label: "stream"
