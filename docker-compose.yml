services:
  # Edge Reverse Proxy & TLS Routing
  proxy:
    image: traefik:v2.10
    container_name: core-proxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./proxy/traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./proxy/traefik/dynamic:/etc/traefik/dynamic:ro
    networks:
      - core-net
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 30s
      timeout: 10s
      retries: 3

  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: core-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: coreplatform
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-localdev123}
      POSTGRES_DB: hyperswitch
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - core-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U coreplatform"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: core-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - core-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # NATS JetStream Event Bus
  nats:
    image: nats:2.9-alpine
    container_name: core-nats
    restart: unless-stopped
    ports:
      - "4222:4222"
      - "8222:8222"
    volumes:
      - ./event-bus/nats/nats-server.conf:/etc/nats/nats-server.conf:ro
      - nats-data:/data
    command: ["-c", "/etc/nats/nats-server.conf"]
    networks:
      - core-net
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8222/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Mock Superposition (Local Dev)
  mock-superposition:
    build:
      context: ./payment-system/hyperswitch/mock-superposition
      dockerfile: Dockerfile
    container_name: core-mock-superposition
    restart: unless-stopped
    ports:
      - "9999:9999"
    networks:
      - core-net
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:9999/')"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Hyperswitch Database Migration
  hyperswitch-migration:
    image: rust:slim-bookworm
    container_name: core-hyperswitch-migration
    environment:
      DATABASE_URL: "postgresql://coreplatform:R3d!sS3cur3%232026@core-postgres:5432/hyperswitch"
    working_dir: /app
    entrypoint: ["/bin/bash", "-c"]
    command:
      - |
        set -e
        echo "=== Installing dependencies ==="
        apt-get update && apt-get install -y --no-install-recommends git libpq-dev pkg-config ca-certificates && rm -rf /var/lib/apt/lists/*
        echo "=== Installing diesel_cli (postgres only) ==="
        cargo install diesel_cli --no-default-features --features postgres
        export PATH="$${HOME}/.cargo/bin:$${PATH}"
        echo "=== Cloning hyperswitch v1.124.0 ==="
        git clone --depth 1 --branch v1.124.0 https://github.com/juspay/hyperswitch.git /tmp/hyperswitch
        cd /tmp/hyperswitch
        echo "=== Running migrations ==="
        diesel migration run
        echo "=== Migrations completed successfully ==="
    networks:
      - core-net
    depends_on:
      postgres:
        condition: service_healthy

  # Hyperswitch Payment Orchestration
  hyperswitch:
    image: juspaydotin/hyperswitch-router:standalone
    container_name: core-hyperswitch
    restart: unless-stopped
    command: "/local/bin/router -f /local/config/docker_compose.toml"
    ports:
      - "8081:8080"
    env_file:
      - ./payment-system/hyperswitch/.env
    environment:
      RUN_ENV: docker_compose
      ROUTER__MASTER_DATABASE__HOST: "core-postgres"
      ROUTER__MASTER_DATABASE__PORT: 5432
      ROUTER__MASTER_DATABASE__DBNAME: "hyperswitch"
      ROUTER__MASTER_DATABASE__USERNAME: "coreplatform"
      ROUTER__MASTER_DATABASE__PASSWORD: "R3d!sS3cur3#2026"
      ROUTER__SECRETS__JWT_SECRET: "secret"
      ROUTER__SECRETS__ADMIN_API_KEY: "test_admin"
      ROUTER__SECRETS__MASTER_ENC_KEY: "73ad7bbbbc640c845a150f67d058b279849370cd2c1f3c67c4dd6c869213e13a"
      ROUTER__REDIS__HOST: "core-redis"
      ROUTER__REDIS__PORT: 6379
      ROUTER__SUPERPOSITION__ENABLED: "false"
      ROUTER__SUPERPOSITION__ENDPOINT: "http://mock-superposition:9999"
      ROUTER__SUPERPOSITION__TOKEN: "local_token"
      ROUTER__SUPERPOSITION__ORG_ID: "local_org"
      ROUTER__SUPERPOSITION__WORKSPACE_ID: "local_workspace"
      ROUTER__SUPERPOSITION__LOCAL_FILE_PATH: "/local/config/superposition_config.json"
    volumes:
      - ./payment-system/hyperswitch/config:/local/config:ro
    depends_on:
      hyperswitch-migration:
        condition: service_completed_successfully
      mock-superposition:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - core-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s

  # Kill Bill Subscription & Billing
  killbill:
    image: killbill/killbill:0.24.11
    container_name: core-killbill
    restart: unless-stopped
    ports:
      - "8082:8080"
    env_file:
      - ./payment-system/killbill/.env
    volumes:
      - ./payment-system/killbill/config:/etc/killbill/config:ro
      - ./payment-system/killbill/plugins:/var/lib/killbill/plugins:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      nats:
        condition: service_healthy
    networks:
      - core-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/1.0/healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 3

  # NATS-KB Bridge (Reconciliation)
  nats-kb-bridge:
    build:
      context: ./payment-system/nats-kb-bridge
      dockerfile: Dockerfile
    container_name: core-nats-kb-bridge
    restart: unless-stopped
    env_file:
      - ./payment-system/nats-kb-bridge/.env
    depends_on:
      nats:
        condition: service_healthy
      killbill:
        condition: service_healthy
    networks:
      - core-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Tazama Auth Service
  tazama-auth:
    build:
      context: ./monitoring-and-rules/tazama-auth
      dockerfile: Dockerfile
    container_name: core-tazama-auth
    restart: unless-stopped
    ports:
      - "8083:8080"
    env_file:
      - ./monitoring-and-rules/tazama-auth/.env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - core-net
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Tazama Rule Execution Engine
  tazama-rule-exec:
    build:
      context: ./monitoring-and-rules/tazama-rule-exec
      dockerfile: Dockerfile
    container_name: core-tazama-rule-exec
    restart: unless-stopped
    ports:
      - "8084:8080"
    env_file:
      - ./monitoring-and-rules/tazama-rule-exec/.env
    volumes:
      - ./monitoring-and-rules/tazama-rule-exec/rules:/etc/tazama/rules:ro
    depends_on:
      nats:
        condition: service_healthy
      tazama-auth:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - core-net
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Tazama Rule Studio UI
  tazama-rule-studio:
    build:
      context: ./monitoring-and-rules/tazama-rule-studio
      dockerfile: Dockerfile
    container_name: core-tazama-rule-studio
    restart: unless-stopped
    ports:
      - "3002:8080"
    env_file:
      - ./monitoring-and-rules/tazama-rule-studio/.env
    depends_on:
      tazama-rule-exec:
        condition: service_healthy
    networks:
      - core-net

  # Case Management Dashboard
  case-management:
    build:
      context: ./monitoring-and-rules/case-management
      dockerfile: Dockerfile
    container_name: core-case-management
    restart: unless-stopped
    ports:
      - "3001:8080"
    env_file:
      - ./monitoring-and-rules/case-management/.env
    depends_on:
      tazama-rule-exec:
        condition: service_healthy
    networks:
      - core-net

  # --- FREE MONITORING SERVICES FOR TESTING ---
  prometheus:
    image: prom/prometheus:latest
    container_name: core-prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus:/etc/prometheus
      - prometheus-data:/prometheus
    networks:
      - core-net
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--web.enable-admin-api'
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:9090"]
      interval: 30s
      timeout: 10s
      retries: 3

  grafana:
    image: grafana/grafana:latest
    container_name: core-grafana
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_SECURITY_ADMIN_USER=admin
    volumes:
      - ./monitoring/grafana:/etc/grafana/provisioning
      - grafana-data:/var/lib/grafana
    networks:
      - core-net
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3

  loki:
    image: grafana/loki:latest
    container_name: core-loki
    restart: unless-stopped
    ports:
      - "3100:3100"
    volumes:
      - ./monitoring/loki:/etc/loki
      - loki-data:/tmp/loki
    networks:
      - core-net
    command: -config.file=/etc/loki/loki.yml
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3100"]
      interval: 30s
      timeout: 10s
      retries: 3

  promtail:
    image: grafana/promtail:latest
    container_name: core-promtail
    restart: unless-stopped
    volumes:
      - ./monitoring/promtail:/etc/promtail
      - /var/log:/var/log:ro
      - promtail-data:/tmp/promtail
    networks:
      - core-net
    command: -config.file=/etc/promtail/config.yml
    depends_on:
      - loki

volumes:
  postgres-data:
  redis-data:
  nats-data:
  prometheus-data:
  grafana-data:
  loki-data:
  promtail-data:

networks:
  core-net:
    driver: bridge